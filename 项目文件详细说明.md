# 智能法律助手 — 项目全部文件详细说明

---

## 一、Python 后端核心文件

### 1. `app.py` — FastAPI 主应用入口

**作用：** 整个系统的 Web 服务主文件，负责路由定义、用户认证、数据库模型、API 接口、文件上传、流式问答等所有 Web 层功能。

**详细功能：**

- **数据库模型定义（SQLAlchemy ORM）：**
  - `User` 用户表：手机号、用户名、密码哈希（bcrypt）、角色（user/expert/admin）、创建时间
  - `KnowledgeBase` 知识库表：名称、描述、所属用户、创建/更新时间
  - `Chat` 对话表：标题、所属用户、关联知识库、创建/更新时间
  - `Message` 消息表：角色（user/bot）、内容、所属对话、创建时间
  - `UploadedDocument` 上传文档表：文件名、文件路径、文件类型、文件大小、所属用户、所属知识库

- **用户认证系统：**
  - Cookie-based 会话认证（非 JWT），token 格式为 `user_id:随机hex`
  - `get_current_user()` 从 cookie 解析用户
  - `require_user()` 强制登录装饰器
  - 密码使用 bcrypt 加密存储
  - 会话有效期 24 小时，支持"记住登录"

- **页面路由（返回 HTML）：**
  - `GET /login` — 登录页面
  - `POST /login` — 登录提交（手机号 + 密码）
  - `GET /register` — 注册页面
  - `POST /register` — 注册提交（手机号 + 用户名 + 密码 + 角色选择）
  - `GET /logout` — 退出登录（清除 cookie）
  - `GET /` — 聊天主页（需登录）
  - `GET /knowledge-bases` — 知识库列表页
  - `GET /knowledge-base/create` — 新建知识库页
  - `POST /knowledge-base/create` — 创建知识库提交
  - `GET /knowledge-base/{kb_id}/edit` — 编辑知识库页
  - `POST /knowledge-base/{kb_id}/edit` — 编辑知识库提交
  - `POST /knowledge-base/{kb_id}/delete` — 删除知识库（同时删除关联文件）
  - `GET /upload` — 文档上传页（仅 expert/admin 角色）
  - `POST /upload` — 文档上传提交（支持 PDF/DOCX/TXT/JPG/PNG，最大 10MB）
  - `POST /upload/{doc_id}/delete` — 删除已上传文档

- **API 路由（返回 JSON）：**
  - `GET /api/chats` — 获取当前用户所有对话列表
  - `GET /api/chats/{chat_id}` — 获取单个对话的所有消息
  - `POST /api/chats` — 创建新对话
  - `PUT /api/chats/{chat_id}` — 更新对话标题或关联知识库
  - `DELETE /api/chats/{chat_id}` — 删除对话（同时清除 RAG 记忆）
  - `POST /api/chats/{chat_id}/clear_memory` — 清空对话的 RAG 记忆
  - `GET /api/knowledge-bases` — 获取当前用户知识库列表

- **流式问答接口：**
  - `GET|POST /ask_stream` — SSE 流式问答端点
  - 接收参数：`user_input`（用户问题）、`chat_id`（对话ID）、`knowledge_base_id`（可选，知识库ID）
  - 通过 `knowledge_base_id` 参数传递给 RAG 模型做 metadata 过滤（不创建临时实例）
  - 使用 `StreamingResponse` 以 SSE（Server-Sent Events）格式逐 token 推送回答
  - 流式完成后将 bot 回复保存到数据库

- **启动初始化：**
  - `initialize_vector_database()` 在应用启动时执行
  - 如果向量数据库不存在，自动扫描 `knowledge_base/` 目录构建索引
  - 同时处理数据库中已上传的文档（统一通过 `DocumentProcessor._load_documents()` 加载，支持 OCR 回退）

- **文档处理优化：**
  - `_process_document_async()` — 使用 FastAPI `BackgroundTasks` 异步处理文档索引
  - 文档处理完成后调用 `rag_model.invalidate_kb_cache()` 清除知识库缓存
  - 删除文档时同步清除 BM25 索引和知识库缓存

---

### 2. `model_utils.py` — RAG 核心引擎

**作用：** RAG（检索增强生成）的核心实现，封装了嵌入模型、向量数据库、混合检索、重排序、查询分析、LLM 调用、对话记忆等全部 RAG 流程。

**核心类：`DeepSeekApiRag`**

- **初始化 `__init__()`：**
  - 嵌入模型初始化：支持本地 `bge-small-zh-v1.5`（HuggingFace + CUDA）或 DashScope API 两种模式
  - LLM 初始化：优先使用 MiMo API，回退到 DeepSeek API
  - 向量数据库初始化：FAISS，支持从本地加载已有索引
  - BM25 检索器初始化
  - 文档处理器初始化
  - Reranker 配置（DashScope gte-rerank）
  - 对话记忆模块初始化（内存缓存 + DB 回退，最近 5 轮）
  - 检索权重配置（默认向量 0.6 + BM25 0.4）
  - **Prompt 模板缓存**（`_prompts_cache`，启动时一次性加载 `prompts.yaml`）
  - **知识库文本缓存**（`_kb_texts_cache`，首次访问时填充）

- **查询分析 `analyze_query()`（单次 LLM 调用）：**
  - 接受 `conversation_history` 参数（由 `generate_response_stream` 传入，避免重复获取）
  - 一次 LLM 调用完成四项任务：多轮对话融合 + 术语改写 + 查询分解 + HyDE 文档生成
  - 返回结构化 JSON：`rewritten_query`（改写查询）、`sub_queries`（子查询列表）、`hypothetical_doc`（假设性文档）
  - 失败时回退到原始查询

- **嵌入生成 `_embed_with_retry()`：**
  - 带重试机制（最多 3 次，指数退避）
  - 主模型失败自动切换到回退模型

- **文档管理：**
  - `add_documents()` — 将文本列表添加到向量数据库和 BM25 索引
  - `add_file_documents()` — 处理单个文件（自动识别法律/通用文档，调用不同分块策略）
  - `add_folder_documents()` — 批量处理文件夹中所有文档（支持图片格式）
  - `_fallback_add_documents()` — 文档处理失败时的回退方案（统一通过 DocumentProcessor 加载）
  - `save_vector_db()` / `load_vector_db()` — 向量数据库持久化

- **混合检索 `hybrid_retrieve_documents()`：**
  - 向量检索：FAISS similarity_search_with_score，返回 top_k*2 候选
  - BM25 检索：关键词匹配，返回 top_k*2 候选
  - 分数归一化：将两种检索的分数映射到 [0,1] 区间
  - 加权融合：同一文档被两种方法检索到时取加权平均
  - 按融合分数降序排列，返回 top_k*2 结果

- **重排序 `_rerank_documents()`：**
  - 调用 DashScope 的 `TextReRank` API
  - **10 秒超时保护**（ThreadPoolExecutor + future.result(timeout=10)）
  - 超时后跳过重排序，返回原始结果
  - 未配置 API Key 时跳过重排序

- **并行检索 `retrieve_documents()`：**
  - 主查询 + 子查询 + HyDE 文档通过 `ThreadPoolExecutor` 并发执行混合检索
  - 合并所有子查询结果，去重
  - 重排序 + 相关性阈值过滤（`RELEVANCE_THRESHOLD`，默认 0.15）
  - 支持 `knowledge_base_id` 参数做 metadata 过滤

- **知识库缓存：**
  - `_get_knowledge_base_texts()` — 首次访问时从磁盘读取并缓存
  - `invalidate_kb_cache()` — 文档增删后清除缓存

- **流式回答生成 `generate_response_stream()`：**
  - 获取一次对话历史，传递给 `analyze_query()` 避免重复查询
  - 调用 `analyze_query()` 获取改写查询、子查询、HyDE 文档
  - 调用 `retrieve_documents()` 并行检索
  - 构建上下文：检索文档（带 `[来源N]` 引用编号）+ 对话历史
  - 从 `prompts.yaml` 加载提示词模板并格式化
  - 调用 LLM 流式接口 `self.llm.stream(prompt)`
  - 保存用户消息到对话记忆
  - 返回流对象和元数据

- **对话记忆管理：**
  - `save_bot_response()` — 保存 AI 回复到内存记忆
  - `clear_conversation_memory()` — 清空指定对话的记忆

- **统计信息：**
  - `get_document_count()` — 向量数据库文档数量
  - `get_bm25_document_count()` — BM25 索引文档数量

---

### 3. `BM25Retriever.py` — BM25 关键词检索器

**作用：** 基于 BM25Okapi 算法的中文关键词检索模块，使用 jieba 分词，支持增量添加文档和批量重建索引。

**核心类：`BM25Retriever`**

- **初始化参数：**
  - `bm25_index_path` — 索引持久化文件路径（默认 `bm25_index.pkl`）
  - `rebuild_threshold` — 累积多少待处理文档后触发索引重建（默认 50）

- **中文分词 `chinese_tokenize()`：** 使用 jieba 分词器

- **索引构建 `build_index()`：** 对所有文档进行 jieba 分词，构建 BM25Okapi 模型

- **增量添加 `add_documents()`：** 新文档先加入 `pending_documents` 缓存队列，达到阈值时自动触发重建

- **搜索 `search()`：**
  - 仅在无索引且有待处理文档时才构建（不在搜索时强制重建）
  - 查询文本经 jieba 分词后计算 BM25 分数
  - 返回按分数降序排列的 top_k 结果

- **持久化：**
  - `save_index()` — 使用 pickle 保存 BM25 模型、文档列表、分词结果
  - `load_index()` — 从 pickle 文件加载索引

- **管理方法：**
  - `force_rebuild()` — 强制立即重建索引（上传文档后调用）
  - `get_document_count()` — 总文档数（含待处理）
  - `remove_documents()` — 从索引中移除指定文本（文档删除时调用）

---

### 4. `DocumentProcessor.py` — 文档处理器（支持 OCR）

**作用：** 自动识别文档类型（法律文档 vs 通用文档），采用不同的分块策略处理。支持扫描版 PDF 和图片文件的 OCR 预处理。

**核心类：`DocumentProcessor`**

- **初始化：**
  - 法律分块器 `DocumentSplitter`（400 字符块，30 字符重叠）
  - 通用分块器 `GeneralDocumentSplitter`（200 字符块，20 字符重叠）
  - `_ocr_engine = None` — PaddleOCR 引擎（延迟加载）

- **文档类型判断 `is_legal_document()`：**
  - 第一层：检查文件名是否包含法律关键词（'法'、'条例'、'规定'、'办法'、'细则'、'章程'、'规范'、'法律'）
  - 第二层：检查文件内容是否包含法律特征（正则匹配条款编号格式如"第X条"、书名号《》、章节标题等）
  - 内容检查通过 `_load_file_content()` 加载，支持 OCR 回退

- **OCR 相关方法：**
  - `_get_ocr_engine()` — 延迟加载 PaddleOCR 引擎（首次扫描文档时才初始化，`lang='ch'`, `use_angle_cls=True`）
  - `_needs_ocr(pages)` — 检测 PDF 文本层是否不足（平均每页 < 50 字符触发 OCR）
  - `_pdf_to_images(file_path)` — 使用 `pdf2image`（DPI=200）将 PDF 转为 PIL Image 列表
  - `_ocr_images_to_documents(images, file_path)` — 对图片列表执行 PaddleOCR，返回 LangChain Document 列表（与 PyPDFLoader 输出结构一致）
  - `_load_image_with_ocr(file_path)` — 对单张图片执行 OCR

- **文件加载 `_load_documents()`（统一入口）：**
  - `.txt` — TextLoader（UTF-8）
  - `.pdf` — 先尝试 PyPDFLoader 提取文本层，若 `_needs_ocr()` 返回 True 则自动触发 OCR 回退
  - `.doc/.docx` — Docx2txtLoader
  - `.jpg/.jpeg/.png/.bmp/.tiff` — 直接 OCR

- **法律文档处理 `process_legal_document()`：**
  - 加载文档内容 → `_extract_structured_articles()` 提取结构化条款
  - 提取法律名称（从书名号《》中）
  - 按"第X条"正则分割条款，每个条款生成结构化数据

- **通用文档处理 `process_general_document()`：**
  - 使用 `GeneralDocumentSplitter`（递归字符分割器）

---

### 5. `DocumentSplitter.py` — 文本分块器

**作用：** 提供两种文本分块策略：法律条款分块和通用递归字符分块。

**类一：`DocumentSplitter`（继承 LangChain `TextSplitter`）**

- **法律条款分块 `split_text()`：**
  - 使用正则 `第[零一二三四五六七八九十百千万\d]+条` 匹配所有条款位置
  - 按条款边界切分文本
  - 单个条款超过 `chunk_size`（默认 500）时，按句号/分号进一步细分 `_split_long_article()`
  - 如果未找到条款格式，回退到通用递归字符分块 `_fallback_split()`

- **长条款细分 `_split_long_article()`：**
  - 按句号（。）和分号（；）分割为句子
  - 贪心合并：当前块 + 新句子不超过 chunk_size 时合并，否则开始新块

**类二：`GeneralDocumentSplitter`**

- 封装 LangChain 的 `RecursiveCharacterTextSplitter`
- 默认 200 字符块，20 字符重叠
- 提供 `split_text()` 和 `split_documents()` 两个接口

---

### 6. `ConversationMemory.py` — 对话记忆管理

**作用：** 管理多轮对话的短期记忆，支持内存缓存 + 数据库回退加载。

**核心类：`ConversationMemory`**

- **初始化：**
  - `max_history_turns` — 最大保留对话轮数（默认 5 轮）
  - `conversations` — 字典，key 为 `conversation_id`，value 为对话历史列表

- **消息管理：**
  - `add_message()` — 添加消息（role + content + timestamp）
  - 自动裁剪：超过 `max_history_turns * 2` 条消息时，只保留最近的
  - `get_recent_history()` — 获取原始历史列表（优先内存，回退到 DB）
  - `get_formatted_history()` — 获取格式化的历史文本（"用户: xxx\n助手: xxx"）
  - `clear_conversation()` — 清空指定对话的记忆

- **DB 回退 `_load_from_db()`：**
  - 内存无数据时，从 `Message` 表查询最近 N 条消息
  - 加载后缓存到内存，避免重复查询
  - `conversation_id` 格式为 `chat_{id}`，可直接用 chat_id 查表

---

## 二、配置文件

### 7. `.env` — 环境变量配置

**作用：** 存储所有敏感配置和系统参数，通过 `python-dotenv` 加载。

**配置项：**

| 配置项 | 说明 |
|--------|------|
| `MIMO_API_KEY` | MiMo LLM API 密钥（必填） |
| `MIMO_BASE_URL` | MiMo API 地址 |
| `MIMO_MODEL` | MiMo 模型名称（默认 mimo-v2.5-pro） |
| `DEEPSEEK_API_KEY` | DeepSeek 备选 LLM 密钥 |
| `EMBEDDING_PROVIDER` | 嵌入模型提供者：`local`（本地）或 `dashscope`（API） |
| `EMBEDDING_MODEL` | 嵌入模型名称/路径（默认 bge-small-zh-v1.5） |
| `VECTOR_DB_PATH` | FAISS 向量数据库存储路径 |
| `RERANKER_API_KEY` | DashScope 重排序 API 密钥 |
| `RERANKER_BASE_URL` | DashScope 重排序 API 地址 |
| `RERANKER_MODEL` | 重排序模型名称（默认 gte-rerank） |
| `VECTOR_RETRIEVAL_WEIGHT` | 向量检索权重（默认 0.6） |
| `BM25_RETRIEVAL_WEIGHT` | BM25 检索权重（默认 0.4） |
| `RELEVANCE_THRESHOLD` | 检索相关性阈值（默认 0.15） |
| `DATABASE_URL` | SQLite 数据库路径 |

---

### 8. `prompts.yaml` — LLM 提示词模板

**作用：** 存储发送给 LLM 的系统提示词模板。

**模板（2 个）：**

1. **`legal_advisor_prompt`** — 法律顾问回答模板
   - 变量：`{query}`、`{context}`、`{conversation_history}`
   - 要求：严格基于参考资料回答，分点式回答，引用来源 `[来源N]`，专业+通俗

2. **`query_analysis_prompt`** — 查询分析模板（单次 LLM 调用）
   - 变量：`{query}`、`{conversation_history}`
   - 输出 JSON：`rewritten_query`（术语化改写）、`sub_queries`（子查询分解）、`hypothetical_doc`（HyDE 假设性文档）

---

### 9. `requirements.txt` — Python 依赖清单

**作用：** 列出项目所有 Python 依赖包及版本要求。

**依赖分类：**
- Web 框架：fastapi、uvicorn、python-multipart、Jinja2
- 数据库：SQLAlchemy
- 认证：bcrypt
- 配置：python-dotenv
- LLM/RAG：langchain 全家桶
- 嵌入与向量库：sentence-transformers、faiss-cpu
- 文档处理：jieba、rank-bm25、docx2txt、pypdf
- OCR 处理：paddlepaddle、paddleocr、pdf2image、Pillow
- PyTorch 需单独安装（根据 CUDA 版本）

---

### 10. `.python-version` — Python 解释器路径

**作用：** 指定项目使用的 Python 解释器绝对路径（`E:\anaconda3\envs\pytorch\python.exe`），供 IDE 和工具链识别正确的 Python 环境。

---

### 11. `start.bat` — Windows 一键启动脚本

**作用：** Windows 批处理脚本，用于一键启动 FastAPI 服务器。

**执行流程：**
1. 设置控制台编码为 UTF-8
2. 配置 Python 解释器路径、项目目录、主机和端口
3. 检查 Python 解释器是否存在
4. 切换到项目目录
5. 启动 3 秒后自动打开浏览器访问 `http://127.0.0.1:8080/login`
6. 运行 `uvicorn app:app --host 127.0.0.1 --port 8080`

---

### 12. `.gitignore` — Git 忽略规则

**作用：** 定义 Git 版本控制中需要忽略的文件和目录。

**主要忽略项：**
- `__pycache__/` — Python 字节码缓存
- `.env` — 环境变量（含密钥）
- `instance/` — SQLite 数据库目录
- `law_faiss/` — FAISS 向量索引（42MB）
- `bm25_index.pkl` — BM25 索引缓存
- `uploads/` — 用户上传文件
- `bge-small-zh-v1.5/` — 本地嵌入模型（367MB）
- `user.db` — 用户数据库
- `.claude/`、`.superpowers/` — AI 工具配置

---

## 三、HTML 模板文件（templates/）

### 13. `templates/base.html` — 基础布局模板

**作用：** 所有页面的基础 HTML 模板，定义了 HTML 骨架、meta 标签、CSS 引用和 Jinja2 块占位符。

**块定义：**
- `{% block title %}` — 页面标题（默认"智能法律助手"）
- `{% block head %}` — 额外 CSS/JS
- `{% block body %}` — 页面主体内容
- `{% block scripts %}` — 额外脚本

**引入资源：** `/static/css/shared.css`（设计系统）

---

### 14. `templates/login.html` — 登录页面

**作用：** 用户登录页面，左右分栏布局。

**布局：**
- 左侧：品牌展示区（天平图标 + "智能法律助手" + 系统简介），主色调背景
- 右侧：登录表单（手机号 + 密码 + 记住登录 + 登录按钮 + 注册链接）

---

### 15. `templates/register.html` — 注册页面

**作用：** 用户注册页面，与登录页类似的左右分栏布局。

**表单字段：** 手机号、用户名、密码（至少 6 位）、确认密码、用户类型选择（普通用户/法律专家/管理员）

---

### 16. `templates/index.html` — 聊天主页面

**作用：** 系统核心页面，包含侧边栏和聊天区域的完整聊天界面。

**布局结构：**

- **侧边栏（280px）：** 品牌标识、新建对话按钮、导航菜单、对话历史列表、用户信息区
- **顶部栏：** 当前对话标题、知识库选择下拉框
- **消息区域：** 用户消息（蓝色气泡）、AI 消息（白色气泡）、流式光标动画
- **输入区域：** 自适应高度文本框、发送按钮

---

### 17. `templates/upload.html` — 文档上传页面

**作用：** 文档管理页面，仅 expert/admin 角色可访问。

**功能区域一 — 上传文档：**
- 目标知识库选择下拉框
- 文件拖放区域（支持拖放和点击浏览）
- 文件信息显示（文件名、大小）
- 上传按钮（提交后禁用并显示"上传中..."）
- 支持格式：PDF、DOCX、TXT、JPG、PNG、BMP、TIFF，最大 10MB

**功能区域二 — 上传记录：**
- 已上传文档列表
- 每个文档显示：文件类型图标（PDF/DOCX/TXT/图片）、文件名、格式、大小、上传时间、所属知识库标签
- 删除按钮（带确认弹窗）

---

### 18. `templates/knowledge_base.html` — 知识库列表页面

**作用：** 展示当前用户的所有知识库，支持创建、编辑、删除操作。

---

### 19. `templates/create_knowledge_base.html` — 新建知识库页面

**作用：** 创建新知识库的表单页面（名称 + 描述）。

---

### 20. `templates/edit_knowledge_base.html` — 编辑知识库页面

**作用：** 编辑已有知识库信息，并展示知识库中的文档列表。

---

## 四、前端静态资源（static/）

### 21. `static/css/shared.css` — 全局设计系统

**作用：** 定义整个项目的 CSS 设计系统，采用"典藏法律"风格——温暖、权威、精致。

**设计规范：**

- 色彩系统：暖色背景（#F5F0E8）、深蓝主色（#1B2A4A）、金色点缀（#C8A45C）
- 字体：思源宋体（Noto Serif SC）用于标题，思源黑体（Noto Sans SC）用于正文
- 间距、圆角、阴影、过渡动画等 4 级系统
- 按钮、表单、卡片、提示消息等组件样式

---

### 22. `static/css/index.css` — 聊天界面样式

**作用：** 聊天主页面的专用样式（侧边栏、消息区域、输入区域）。

---

### 23. `static/js/index.js` — 聊天交互逻辑

**作用：** 聊天页面的全部前端 JavaScript 逻辑。

**核心功能：**

- 对话管理：创建、加载、删除、更新标题
- 消息发送：POST 到 `/ask_stream`
- SSE 流式接收：逐行解析 `data: {...}`，实时更新消息
- 知识库选择：绑定/解绑知识库到对话
- UI 交互：Enter 发送、自动高度、侧边栏展开/收起

---

## 五、数据目录

### 24. `knowledge_base/` — 法律文本知识库

**作用：** 存放法律全文文本文件（`.txt` 格式），是 RAG 系统的知识来源。

**内容：** 80+ 部中华人民共和国法律全文，包括宪法、民法典、刑法、消费者权益保护法、劳动法、合同法等。

**处理流程：** 首次启动时自动扫描 → DocumentProcessor 识别为法律文档 → 按条款分块 → 嵌入向量化 → 存入 FAISS + BM25 索引

---

### 25. `bge-small-zh-v1.5/` — 本地嵌入模型（.gitignore 排除）

**作用：** 本地部署的中文文本嵌入模型 `bge-small-zh-v1.5`（BAAI 智源研究院），512 维输出，需 CUDA GPU。

---

### 26. `law_faiss/` — FAISS 向量索引（自动生成，.gitignore 排除）

**作用：** FAISS 向量数据库持久化文件。删除后重启服务会自动从 `knowledge_base/` 重建。

---

### 27. `bm25_index.pkl` — BM25 索引缓存（自动生成，.gitignore 排除）

**作用：** BM25 关键词索引的 pickle 持久化文件。删除后重启服务会自动重建。

---

### 28. `uploads/` — 用户上传文档目录（.gitignore 排除）

**作用：** 存放通过网页上传的文档文件。上传时使用 UUID 重命名，原始文件名保存在数据库中。

**支持格式：** PDF、DOCX、TXT、JPG、JPEG、PNG、BMP、TIFF

---

### 29. `instance/user.db` — SQLite 数据库（.gitignore 排除）

**作用：** 存储所有持久化数据的 SQLite 数据库文件。

**数据表：** user、knowledge_base、chat、message、uploaded_document

---

## 六、测试目录（test/）

### 30. `test/run_all.py` — 测试运行器

**作用：** 统一运行所有测试脚本。

**运行方式：**

- `python test/run_all.py` — 仅离线测试
- `python test/run_all.py --online` — 包含需要服务器的在线测试

---

### 31. `test/test_ocr.py` — OCR 文档预处理测试

**作用：** 测试 DocumentProcessor 的 OCR 回退逻辑（无需启动服务器）。

**测试项：**

- `test_needs_ocr` — 文本层检测逻辑（空页面、文本充足、文本不足、混合情况）
- `test_ocr_engine_lazy_load` — PaddleOCR 引擎延迟加载和缓存
- `test_ocr_text_pdf` — 文本 PDF 走快速路径（不触发 OCR）
- `test_legal_detection` — 法律文档检测（含 OCR 回退场景）
- `test_process_document` — 完整文档处理流程
- `test_image_ocr` — 图片 OCR 识别

---

### 32. `test/baseline_eval.py` — RAG 基准测试

**作用：** 通过 HTTP 请求测试 RAG 系统的检索和生成效果（需要服务器运行）。

**测试用例（10 个）：**

- R1-R5：检索质量测试（直接查询、口语化查询、模糊查询、复合问题、冷门法律）
- M1-M2：多轮对话测试（指代消解、追问深入）
- E1-E3：边界情况测试（无关问题、超短查询、跨法律查询）

**输出：** `test/baseline_results.json`（测试结果数据）

---

### 33. `test/baseline_record.md` — 历次基准测试结果记录

**作用：** 记录每次基准测试的指标数据，用于对比改进效果。

---

## 七、项目文档

### 34. `README.md` — 项目说明文档

**作用：** 项目的完整部署指南和功能说明。

---

### 35. `CLAUDE.md` — Claude Code 指导文件

**作用：** 为 Claude Code AI 助手提供项目上下文，帮助其快速理解项目架构和开发规范。

---

### 36. `改进计划.md` — RAG 流水线改进计划

**作用：** 记录 13 项改进计划的进度和实现细节。

**已完成：** P0-P2 + P13 + 响应时间优化 + PaddleOCR 文档预处理
**待做：** Faithfulness Check、用户反馈闭环、R2 口语化查询回退优化

---

### 37. `LICENSE` — 开源许可证

---

## 八、数据流向总览

```text
用户浏览器
    │
    ▼
[templates/*.html + static/css/*.css + static/js/index.js]  ← 前端界面
    │
    ▼
[app.py]  ← FastAPI 路由、认证、数据库
    │
    ├── /ask_stream (SSE 流式)
    │       │
    │       ▼
    │   [model_utils.py - DeepSeekApiRag]
    │       │
    │       ├── analyze_query()  ← 单次 LLM 调用：改写+分解+HyDE
    │       │
    │       ├── ThreadPoolExecutor 并行检索
    │       │   ├── FAISS 向量检索 (law_faiss/)
    │       │   ├── BM25 关键词检索 (BM25Retriever.py, bm25_index.pkl)
    │       │   └── 子查询 + HyDE 文档检索
    │       │
    │       ├── DashScope Reranker API（10s 超时）
    │       ├── 相关性阈值过滤 (RELEVANCE_THRESHOLD)
    │       ├── 知识库 metadata 过滤 (knowledge_base_id)
    │       │
    │       ├── [prompts.yaml]  ← 提示词模板（缓存）
    │       ├── [ConversationMemory.py]  ← 对话记忆（内存+DB回退）
    │       └── MiMo / DeepSeek LLM API
    │
    ├── /upload → [DocumentProcessor.py]  ← OCR 感知文档处理
    │       ├── 文本 PDF → PyPDFLoader（快速路径）
    │       ├── 扫描 PDF → pdf2image → PaddleOCR（OCR 回退）
    │       ├── 图片文件 → PaddleOCR
    │       └── → [uploads/] + [instance/user.db]
    │
    └── /knowledge-base/* → [instance/user.db]
```
