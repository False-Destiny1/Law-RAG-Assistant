# 智能法律助手 — 项目全部文件详细说明

---

## 一、Python 后端核心文件

### 1. `app.py` — FastAPI 主应用入口

**作用：** 整个系统的 Web 服务主文件，负责路由定义、用户认证、数据库模型、API 接口、文件上传、流式问答等所有 Web 层功能。

**详细功能：**

- **数据库模型定义（SQLAlchemy ORM）：**
  - `User` 用户表：手机号、用户名、密码哈希（bcrypt）、角色（user/expert/admin）、创建时间
  - `KnowledgeBase` 知识库表：名称、描述、所属用户、创建/更新时间
  - `Chat` 对话表：标题、所属用户、关联知识库、创建/更新时间
  - `Message` 消息表：角色（user/bot）、内容、所属对话、创建时间
  - `UploadedDocument` 上传文档表：文件名、文件路径、文件类型、文件大小、所属用户、所属知识库

- **用户认证系统：**
  - Cookie-based 会话认证（非 JWT），token 格式为 `user_id:随机hex`
  - `get_current_user()` 从 cookie 解析用户
  - `require_user()` 强制登录装饰器
  - 密码使用 bcrypt 加密存储
  - 会话有效期 24 小时，支持"记住登录"

- **页面路由（返回 HTML）：**
  - `GET /login` — 登录页面
  - `POST /login` — 登录提交（手机号 + 密码）
  - `GET /register` — 注册页面
  - `POST /register` — 注册提交（手机号 + 用户名 + 密码 + 角色选择）
  - `GET /logout` — 退出登录（清除 cookie）
  - `GET /` — 聊天主页（需登录）
  - `GET /knowledge-bases` — 知识库列表页
  - `GET /knowledge-base/create` — 新建知识库页
  - `POST /knowledge-base/create` — 创建知识库提交
  - `GET /knowledge-base/{kb_id}/edit` — 编辑知识库页
  - `POST /knowledge-base/{kb_id}/edit` — 编辑知识库提交
  - `POST /knowledge-base/{kb_id}/delete` — 删除知识库（同时删除关联文件）
  - `GET /upload` — 文档上传页（仅 expert/admin 角色）
  - `POST /upload` — 文档上传提交（支持 PDF/DOCX/TXT，最大 10MB）
  - `POST /upload/{doc_id}/delete` — 删除已上传文档

- **API 路由（返回 JSON）：**
  - `GET /api/chats` — 获取当前用户所有对话列表
  - `GET /api/chats/{chat_id}` — 获取单个对话的所有消息
  - `POST /api/chats` — 创建新对话
  - `PUT /api/chats/{chat_id}` — 更新对话标题或关联知识库
  - `DELETE /api/chats/{chat_id}` — 删除对话（同时清除 RAG 记忆）
  - `POST /api/chats/{chat_id}/clear_memory` — 清空对话的 RAG 记忆
  - `GET /api/knowledge-bases` — 获取当前用户知识库列表

- **流式问答接口：**
  - `GET|POST /ask_stream` — SSE 流式问答端点
  - 接收参数：`user_input`（用户问题）、`chat_id`（对话ID）、`knowledge_base_id`（可选，知识库ID）
  - expert/admin 角色可选择特定知识库，创建临时 RAG 实例处理
  - 普通用户使用全局 RAG 实例
  - 使用 `StreamingResponse` 以 SSE（Server-Sent Events）格式逐 token 推送回答
  - 流式完成后将 bot 回复保存到数据库

- **启动初始化：**
  - `initialize_vector_database()` 在应用启动时执行
  - 如果向量数据库不存在，自动扫描 `knowledge_base/` 目录构建索引
  - 同时处理数据库中已上传的文档

---

### 2. `model_utils.py` — RAG 核心引擎

**作用：** RAG（检索增强生成）的核心实现，封装了嵌入模型、向量数据库、混合检索、重排序、LLM 调用、对话记忆等全部 RAG 流程。

**核心类：`DeepSeekApiRag`**

- **初始化 `__init__()`：**
  - 嵌入模型初始化：支持本地 `bge-small-zh-v1.5`（HuggingFace + CUDA）或 DashScope API 两种模式
  - LLM 初始化：优先使用 MiMo API，回退到 DeepSeek API
  - 向量数据库初始化：FAISS，支持从本地加载已有索引
  - BM25 检索器初始化
  - 文档处理器初始化
  - Reranker 配置（DashScope gte-rerank）
  - 对话记忆模块初始化（最近 5 轮）
  - 检索权重配置（默认向量 0.6 + BM25 0.4）

- **嵌入生成 `_embed_with_retry()`：**
  - 带重试机制（最多 3 次，指数退避）
  - 主模型失败自动切换到回退模型
  - 用于将文本转换为向量

- **文档管理：**
  - `add_documents()` — 将文本列表添加到向量数据库和 BM25 索引
  - `add_file_documents()` — 处理单个文件（自动识别法律/通用文档，调用不同分块策略）
  - `add_folder_documents()` — 批量处理文件夹中所有文档
  - `_fallback_add_documents()` — 文档处理失败时的回退方案（使用通用分块器）
  - `save_vector_db()` / `load_vector_db()` — 向量数据库持久化

- **混合检索 `hybrid_retrieve_documents()`：**
  - 向量检索：FAISS similarity_search_with_score，返回 top_k*2 候选
  - BM25 检索：关键词匹配，返回 top_k*2 候选
  - 分数归一化：将两种检索的分数映射到 [0,1] 区间
  - 加权融合：同一文档被两种方法检索到时取加权平均
  - 按融合分数降序排列，返回 top_k*2 结果

- **重排序 `_rerank_documents()`：**
  - 调用 DashScope 的 `TextReRank` API
  - 输入：用户查询 + 混合检索候选文档（最多 20 个）
  - 输出：精排后的 top_k 结果（带相关性分数）
  - 未配置 API Key 时跳过重排序

- **检索+重排序 `retrieve_documents()`：**
  - 串联混合检索和重排序两个阶段
  - 先混合检索获取 2x 候选，再重排序精排

- **流式回答生成 `generate_response_stream()`：**
  - 调用 `retrieve_documents()` 获取相关文档
  - 构建上下文：检索文档 + 对话历史
  - 从 `prompts.yaml` 加载提示词模板并格式化
  - 调用 LLM 流式接口 `self.llm.stream(prompt)`
  - 保存用户消息到对话记忆
  - 返回流对象和元数据

- **对话记忆管理：**
  - `save_bot_response()` — 保存 AI 回复到内存记忆
  - `clear_conversation_memory()` — 清空指定对话的记忆

- **统计信息：**
  - `get_document_count()` — 向量数据库文档数量
  - `get_bm25_document_count()` — BM25 索引文档数量
  - `get_retrieval_stats()` — 检索系统完整统计

---

### 3. `BM25Retriever.py` — BM25 关键词检索器

**作用：** 基于 BM25Okapi 算法的中文关键词检索模块，使用 jieba 分词，支持增量添加文档和批量重建索引。

**核心类：`BM25Retriever`**

- **初始化参数：**
  - `bm25_index_path` — 索引持久化文件路径（默认 `bm25_index.pkl`）
  - `rebuild_threshold` — 累积多少待处理文档后触发索引重建（默认 50）

- **中文分词 `chinese_tokenize()`：**
  - 使用 jieba 分词器将中文文本切分为词语列表

- **索引构建 `build_index()`：**
  - 对所有文档进行 jieba 分词
  - 构建 BM25Okapi 模型

- **增量添加 `add_documents()`：**
  - 新文档先加入 `pending_documents` 缓存队列
  - 当待处理文档数量达到阈值时自动触发 `_rebuild_with_pending()`
  - 重建时合并已有文档和待处理文档，重新构建完整索引

- **搜索 `search()`：**
  - 搜索前自动处理所有待处理文档
  - 查询文本经 jieba 分词后计算 BM25 分数
  - 返回按分数降序排列的 top_k 结果

- **持久化：**
  - `save_index()` — 使用 pickle 保存 BM25 模型、文档列表、分词结果
  - `load_index()` — 从 pickle 文件加载索引

- **管理方法：**
  - `force_rebuild()` — 强制立即重建索引
  - `get_document_count()` — 总文档数（含待处理）
  - `get_indexed_count()` — 已索引文档数
  - `get_pending_count()` — 待处理文档数
  - `clear_index()` — 清空所有索引数据

---

### 4. `DocumentProcessor.py` — 文档处理器

**作用：** 自动识别文档类型（法律文档 vs 通用文档），并采用不同的分块策略处理。

**核心类：`DocumentProcessor`**

- **文档类型判断 `is_legal_document()`：**
  - 第一层：检查文件名是否包含法律关键词（'法'、'条例'、'规定'、'办法'、'细则'、'章程'、'规范'、'法律'）
  - 第二层：检查文件内容是否包含法律特征（正则匹配条款编号格式如"第X条"、书名号《》、章节标题等）

- **法律文档处理 `process_legal_document()`：**
  - 加载文档内容
  - 调用 `_extract_structured_articles()` 提取结构化条款
  - 提取法律名称（从书名号《》中）
  - 按"第X条"正则分割条款
  - 每个条款生成结构化数据：法律名称、条款编号、条款内容、完整文本
  - 使用 `DocumentSplitter`（按条款分割器，400 字符块，30 字符重叠）

- **通用文档处理 `process_general_document()`：**
  - 使用 `GeneralDocumentSplitter`（递归字符分割器，200 字符块，20 字符重叠）
  - 每个块生成结构化数据：块编号、内容、元数据

- **文件加载 `_load_documents()`：**
  - 支持 `.txt`（TextLoader，UTF-8）、`.pdf`（PyPDFLoader）、`.doc/.docx`（Docx2txtLoader）

---

### 5. `DocumentSplitter.py` — 文本分块器

**作用：** 提供两种文本分块策略：法律条款分块和通用递归字符分块。

**类一：`DocumentSplitter`（继承 LangChain `TextSplitter`）**

- **法律条款分块 `split_text()`：**
  - 使用正则 `第[零一二三四五六七八九十百千万\d]+条` 匹配所有条款位置
  - 按条款边界切分文本
  - 单个条款超过 `chunk_size`（默认 500）时，按句号/分号进一步细分 `_split_long_article()`
  - 如果未找到条款格式，回退到通用递归字符分块 `_fallback_split()`

- **长条款细分 `_split_long_article()`：**
  - 按句号（。）和分号（；）分割为句子
  - 贪心合并：当前块 + 新句子不超过 chunk_size 时合并，否则开始新块

**类二：`GeneralDocumentSplitter`**

- **通用分块：**
  - 封装 LangChain 的 `RecursiveCharacterTextSplitter`
  - 默认 200 字符块，20 字符重叠
  - 提供 `split_text()` 和 `split_documents()` 两个接口

---

### 6. `ConversationMemory.py` — 对话记忆管理

**作用：** 管理多轮对话的短期记忆，保存在内存中（非持久化），用于构建 RAG 上下文时提供对话历史。

**核心类：`ConversationMemory`**

- **初始化：**
  - `max_history_turns` — 最大保留对话轮数（默认 5 轮）
  - `conversations` — 字典，key 为 `conversation_id`，value 为对话历史列表

- **消息管理：**
  - `add_message()` — 添加消息（role + content + timestamp）
  - 自动裁剪：超过 `max_history_turns * 2` 条消息时，只保留最近的
  - `get_recent_history()` — 获取原始历史列表
  - `get_formatted_history()` — 获取格式化的历史文本（"用户: xxx\n助手: xxx"）
  - `clear_conversation()` — 清空指定对话的记忆

- **注意：** 记忆仅保存在内存中，服务重启后丢失。数据库中的 Message 表是持久化存储。

---

## 二、配置文件

### 7. `.env` — 环境变量配置

**作用：** 存储所有敏感配置和系统参数，通过 `python-dotenv` 加载。

**配置项：**

| 配置项 | 说明 |
|--------|------|
| `MIMO_API_KEY` | MiMo LLM API 密钥（必填） |
| `MIMO_BASE_URL` | MiMo API 地址 |
| `MIMO_MODEL` | MiMo 模型名称（默认 mimo-v2.5-pro） |
| `DEEPSEEK_API_KEY` | DeepSeek 备选 LLM 密钥 |
| `EMBEDDING_PROVIDER` | 嵌入模型提供者：`local`（本地）或 `dashscope`（API） |
| `EMBEDDING_MODEL` | 嵌入模型名称/路径（默认 bge-small-zh-v1.5） |
| `VECTOR_DB_PATH` | FAISS 向量数据库存储路径 |
| `RERANKER_API_KEY` | DashScope 重排序 API 密钥 |
| `RERANKER_BASE_URL` | DashScope 重排序 API 地址 |
| `RERANKER_MODEL` | 重排序模型名称（默认 gte-rerank） |
| `VECTOR_RETRIEVAL_WEIGHT` | 向量检索权重（默认 0.6） |
| `BM25_RETRIEVAL_WEIGHT` | BM25 检索权重（默认 0.4） |
| `DATABASE_URL` | SQLite 数据库路径 |

---

### 8. `prompts.yaml` — LLM 提示词模板

**作用：** 存储发送给 LLM 的系统提示词模板，定义 AI 法律顾问的角色和回答规范。

**模板变量：**
- `{query}` — 用户提问
- `{context}` — 检索到的相关文档上下文
- `{conversation_history}` — 对话历史

**回答要求：**
- 严格基于参考资料回答，无相关内容需如实说明
- 使用分点式回答，每个关键点独立成段
- 专业法律术语 + 通俗解释
- 不包含"相关文档"字样

---

### 9. `requirements.txt` — Python 依赖清单

**作用：** 列出项目所有 Python 依赖包及版本要求。

**依赖分类：**
- Web 框架：fastapi、uvicorn、python-multipart、Jinja2
- 数据库：SQLAlchemy
- 认证：bcrypt
- 配置：python-dotenv
- LLM/RAG：langchain 全家桶（langchain、langchain-core、langchain-community、langchain-openai、langchain-huggingface、langchain-text-splitters）
- 嵌入与向量库：sentence-transformers、faiss-cpu
- 文档处理：jieba、rank-bm25、docx2txt、pypdf
- PyTorch 需单独安装（根据 CUDA 版本）

---

### 10. `.python-version` — Python 解释器路径

**作用：** 指定项目使用的 Python 解释器绝对路径（`E:\anaconda3\envs\pytorch\python.exe`），供 IDE 和工具链识别正确的 Python 环境。

---

### 11. `start.bat` — Windows 一键启动脚本

**作用：** Windows 批处理脚本，用于一键启动 FastAPI 服务器。

**执行流程：**
1. 设置控制台编码为 UTF-8
2. 配置 Python 解释器路径、项目目录、主机和端口
3. 检查 Python 解释器是否存在
4. 切换到项目目录
5. 启动 3 秒后自动打开浏览器访问 `http://127.0.0.1:8080/login`
6. 运行 `uvicorn app:app --host 127.0.0.1 --port 8080`

---

### 12. `.gitignore` — Git 忽略规则

**作用：** 定义 Git 版本控制中需要忽略的文件和目录。

**主要忽略项：**
- `__pycache__/` — Python 字节码缓存
- `.env` — 环境变量（含密钥）
- `instance/` — SQLite 数据库目录
- `law_faiss/` — FAISS 向量索引
- `bm25_index.pkl` — BM25 索引缓存
- `uploads/` — 用户上传文件
- 各种 IDE 配置、虚拟环境、构建产物等

---

## 三、HTML 模板文件（templates/）

### 13. `templates/base.html` — 基础布局模板

**作用：** 所有页面的基础 HTML 模板，定义了 HTML 骨架、meta 标签、CSS 引用和 Jinja2 块占位符。

**块定义：**
- `{% block title %}` — 页面标题（默认"智能法律助手"）
- `{% block head %}` — 额外 CSS/JS
- `{% block body %}` — 页面主体内容
- `{% block scripts %}` — 额外脚本

**引入资源：** `/static/css/shared.css`（设计系统）

---

### 14. `templates/login.html` — 登录页面

**作用：** 用户登录页面，左右分栏布局。

**布局：**
- 左侧：品牌展示区（天平图标 + "智能法律助手" + 系统简介），主色调背景
- 右侧：登录表单（手机号 + 密码 + 记住登录 + 登录按钮 + 注册链接）

**功能：**
- 表单 POST 到 `/login`
- 显示错误/成功提示信息
- 响应式：移动端隐藏左侧品牌区

---

### 15. `templates/register.html` — 注册页面

**作用：** 用户注册页面，与登录页类似的左右分栏布局。

**表单字段：**
- 手机号（唯一标识）
- 用户名
- 密码（至少 6 位）
- 确认密码
- 用户类型选择（卡片式单选）：普通用户、法律专家、管理员

**功能：**
- 表单 POST 到 `/register`
- 显示多条错误信息（逐条列出）
- 注册成功后显示提示

---

### 16. `templates/index.html` — 聊天主页面

**作用：** 系统核心页面，包含侧边栏和聊天区域的完整聊天界面。

**布局结构：**
- **侧边栏（280px）：**
  - 品牌标识 + 收起按钮
  - "新建对话"按钮
  - 导航菜单（expert/admin 可见"文档管理"和"知识库"链接）
  - 对话历史列表（动态加载，支持编辑标题和删除）
  - 底部：用户信息（头像首字母 + 用户名 + 角色）+ 退出按钮

- **顶部栏：**
  - 移动端菜单按钮
  - 当前对话标题
  - 知识库选择下拉框（选择当前对话使用的知识库）

- **消息区域：**
  - 默认显示欢迎消息
  - 用户消息右对齐，蓝色气泡
  - AI 消息左对齐，白色气泡
  - 流式输出时显示光标动画

- **输入区域：**
  - 自适应高度文本框（最大 120px）
  - 发送按钮（圆形）
  - 提示文字：Enter 发送，Shift+Enter 换行

**引入资源：** `/static/css/index.css` + `/static/js/index.js`

---

### 17. `templates/upload.html` — 文档上传页面

**作用：** 文档管理页面，仅 expert/admin 角色可访问。

**布局：**
- 左侧导航栏（对话、文档管理[高亮]、知识库）
- 右侧主内容区

**功能区域一 — 上传文档：**
- 目标知识库选择下拉框
- 文件拖放区域（支持拖放和点击浏览）
- 文件信息显示（文件名、大小）
- 上传按钮（提交后禁用并显示"上传中..."）
- 支持格式：PDF、DOCX、TXT，最大 10MB

**功能区域二 — 上传记录：**
- 已上传文档列表
- 每个文档显示：文件类型图标、文件名、格式、大小、上传时间、所属知识库标签
- 删除按钮（带确认弹窗）

---

### 18. `templates/knowledge_base.html` — 知识库列表页面

**作用：** 展示当前用户的所有知识库，支持创建、编辑、删除操作。

**布局：**
- 左侧导航栏
- 右侧主内容区

**功能：**
- 页面标题 + "新建知识库"按钮
- 知识库卡片网格（auto-fill 响应式布局）
- 每张卡片显示：图标、名称、描述、文档数量、更新时间
- 操作按钮：编辑、删除（删除时关联文档也会被删除）
- 空状态：显示引导创建知识库的提示

---

### 19. `templates/create_knowledge_base.html` — 新建知识库页面

**作用：** 创建新知识库的表单页面。

**表单字段：**
- 知识库名称（必填）
- 描述（可选，多行文本框）

**操作按钮：** 取消（返回列表）、创建知识库

---

### 20. `templates/edit_knowledge_base.html` — 编辑知识库页面

**作用：** 编辑已有知识库信息，并展示知识库中的文档列表。

**功能区域一 — 编辑表单：**
- 知识库名称（预填当前值）
- 描述（预填当前值）
- 操作按钮：取消、保存更改

**功能区域二 — 知识库中的文档：**
- 文档列表：文件类型图标、文件名、格式、大小、上传时间
- 空状态：提示前往上传文档

---

## 四、前端静态资源（static/）

### 21. `static/css/shared.css` — 全局设计系统

**作用：** 定义整个项目的 CSS 设计系统（Design System），采用"典藏法律"风格——温暖、权威、精致。

**设计规范：**
- **色彩系统：** 暖色背景（#F5F0E8）、深蓝主色（#1B2A4A）、金色点缀（#C8A45C）
- **字体：** 思源宋体（Noto Serif SC）用于标题，思源黑体（Noto Sans SC）用于正文
- **间距系统：** 0.25rem 到 4rem 的 12 级间距
- **圆角：** 4 级（6px / 10px / 16px / 24px）
- **阴影：** 4 级（sm / md / lg / xl）
- **过渡动画：** 自定义 ease-out 曲线

**组件样式：**
- 按钮系统：primary / accent / outline / ghost / danger，支持 sm / lg / block 尺寸
- 表单元素：input / textarea / select，带焦点状态
- 卡片组件
- 提示消息：error / success / info，带滑入动画
- 表格样式
- 徽章（badge）
- 工具类：间距、flex 布局、文字对齐等
- 页面入场动画（fadeInUp，支持延迟）
- 自定义滚动条
- 纸张纹理背景（SVG noise overlay）

---

### 22. `static/css/index.css` — 聊天界面样式

**作用：** 聊天主页面的专用样式，定义侧边栏、消息区域、输入区域的布局和视觉效果。

**主要样式：**
- **布局：** flex 水平布局，sidebar（280px）+ chat-main（flex:1）
- **侧边栏：** 品牌区、导航菜单、对话列表（hover 显示操作按钮）、用户信息区
- **消息区域：** 消息气泡（bot 白色 / user 蓝色）、头像圆形、流式光标动画（闪烁竖线）、加载动画（三个跳动圆点）
- **输入区域：** 圆角输入框 + 圆形发送按钮，焦点时蓝色边框发光
- **响应式：** 768px 以下侧边栏变为固定定位抽屉式，通过 `.open` 类控制显示

---

### 23. `static/js/index.js` — 聊天交互逻辑

**作用：** 聊天页面的全部前端 JavaScript 逻辑，实现对话管理、消息发送、SSE 流式接收等功能。

**核心功能：**

- **初始化 `init()`：**
  - 获取对话列表 → 加载最近对话或创建新对话
  - 获取知识库列表 → 填充下拉选择器
  - 设置输入框自动高度

- **对话管理：**
  - `fetchChats()` — 从 `/api/chats` 获取对话列表
  - `createNewChat()` — POST 创建新对话，清空聊天框，显示欢迎消息
  - `loadChat(chatId)` — 加载指定对话的所有消息
  - `deleteChat(chatId)` — 删除对话
  - `updateChatTitle(chatId, title)` — 更新对话标题
  - `renderChatList()` — 渲染侧边栏对话列表（按更新时间排序，高亮当前对话）

- **消息发送 `sendMessage()`：**
  - 获取输入文本，显示用户消息气泡
  - 自动用首条消息前 20 字更新对话标题
  - 显示加载动画（三个跳动圆点）
  - POST 到 `/ask_stream`，携带 `user_input`、`chat_id`、`knowledge_base_id`

- **SSE 流式接收 `handleStream()`：**
  - 使用 `ReadableStream` 读取响应流
  - 逐行解析 `data: {...}` 格式的 SSE 消息
  - 实时更新消息内容（带流式光标动画）
  - 解析 JSON 格式的 content 字段
  - 处理 `[DONE]` 和 error 消息
  - 完成后移除光标动画，格式化最终消息

- **UI 交互：**
  - Enter 发送，Shift+Enter 换行
  - 输入框自动高度调整
  - 知识库选择变更时更新当前对话的关联
  - 侧边栏展开/收起（移动端抽屉式）
  - 点击侧边栏外部自动关闭（移动端）

- **辅助函数：**
  - `formatMessage()` — 将纯文本转换为 HTML（换行转 `<p>` 和 `<br>`）
  - `escapeHtml()` — XSS 防护
  - `scrollToBottom()` — 自动滚动到最新消息

---

## 五、数据目录

### 24. `knowledge_base/` — 法律文本知识库

**作用：** 存放法律全文文本文件（`.txt` 格式），是 RAG 系统的知识来源。

**内容：** 80+ 部中华人民共和国法律全文，包括但不限于：
- 宪法、民法典、刑法
- 消费者权益保护法、劳动法、合同法
- 环境保护类法律（水、大气、土壤、噪声等污染防治法）
- 经济类法律（商业银行法、反洗钱法、反垄断法等）
- 社会类法律（未成年人保护法、妇女权益保障法、残疾人保障法等）

**命名格式：** `中华人民共和国XXX法.txt`

**处理流程：** 首次启动时自动扫描此目录 → DocumentProcessor 识别为法律文档 → 按条款分块 → 嵌入向量化 → 存入 FAISS + BM25 索引

---

### 25. `bge-small-zh-v1.5/` — 本地嵌入模型

**作用：** 存放本地部署的中文文本嵌入模型 `bge-small-zh-v1.5`（由 BAAI 智源研究院开发）。

**技术细节：**
- 基于 BERT 架构的中文语义嵌入模型
- 输出维度：512
- 通过 HuggingFace `sentence-transformers` 加载
- 需要 CUDA GPU 支持
- 用于将文本转换为向量，支持语义相似度计算

---

### 26. `law_faiss/` — FAISS 向量索引（自动生成）

**作用：** 存放 FAISS 向量数据库的持久化文件，首次构建后自动生成。

**内容：**
- FAISS 索引文件（`.faiss`）
- 文档映射文件（`.pkl`）

**说明：** 删除此目录后重启服务会自动从 `knowledge_base/` 重建。

---

### 27. `bm25_index.pkl` — BM25 索引缓存（自动生成）

**作用：** BM25 关键词索引的 pickle 持久化文件。

**内容：** BM25Okapi 模型对象、完整文档列表、分词结果列表。

**说明：** 删除后重启服务会自动重建。

---

### 28. `uploads/` — 用户上传文档目录

**作用：** 存放通过网页上传的文档文件。

**文件命名：** 上传时使用 UUID 重命名（如 `a1b2c3d4.pdf`），原始文件名保存在数据库中。

**支持格式：** PDF、DOCX、TXT

---

### 29. `instance/user.db` — SQLite 数据库

**作用：** 存储所有持久化数据的 SQLite 数据库文件。

**数据表：**
- `user` — 用户账号信息
- `knowledge_base` — 知识库定义
- `chat` — 对话记录
- `message` — 消息内容（user 和 bot 的每条消息）
- `uploaded_document` — 上传文档元数据

---

## 六、自动生成的文件

### 30. `README.md` — 项目说明文档

**作用：** 项目的完整部署指南和功能说明。

**内容：**
- 项目概述和技术栈
- 完整的 6 步部署流程（环境准备 → 安装依赖 → 配置环境变量 → 准备知识库 → 首次启动 → 访问系统）
- 项目目录结构图
- 核心架构流程图
- 功能说明表（含权限说明）
- 常见问题排查

---

### 31. `CLAUDE.md` — Claude Code 指导文件

**作用：** 为 Claude Code AI 助手提供项目上下文，帮助其快速理解项目架构和开发规范。

---

## 七、数据流向总览

```
用户浏览器
    │
    ▼
[templates/*.html + static/css/*.css + static/js/index.js]  ← 前端界面
    │
    ▼
[app.py]  ← FastAPI 路由、认证、数据库
    │
    ├── /ask_stream (SSE 流式)
    │       │
    │       ▼
    │   [model_utils.py - DeepSeekApiRag]
    │       │
    │       ├── [BM25Retriever.py]  ← 关键词检索 (jieba 分词)
    │       ├── [DocumentProcessor.py]  ← 文档类型识别
    │       ├── [DocumentSplitter.py]  ← 文本分块
    │       ├── [ConversationMemory.py]  ← 对话记忆
    │       ├── [prompts.yaml]  ← 提示词模板
    │       │
    │       ├── FAISS 向量检索 (law_faiss/)
    │       ├── BM25 检索 (bm25_index.pkl)
    │       ├── DashScope Reranker API
    │       └── MiMo / DeepSeek LLM API
    │
    ├── /upload → [uploads/] + [instance/user.db]
    └── /knowledge-base/* → [instance/user.db]
```
